enum EAmbiente {
  DESENVOLVIMENTO,
  PRODUCAO,
}

enum ETipoLogin {
  NAO_LOGADO,
  EMAIL,
  FACEBOOK,
  GOOGLE,
}

enum ESessao {
  USUARIO_LOGADO,
  USUARIO_DESLOGADO,
}

enum EConexao {
  NENHUMA,
  MOBILE,
  WIFI,
}
